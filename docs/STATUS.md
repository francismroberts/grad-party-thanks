# Status — updated 2026-09-11 07:33 PT

## Built this session
- Repo scaffold, `CLAUDE.md`, `scripts/ingest.js` (stage 2), `scripts/serve.mjs` (local preview, listens on the LAN and prints the phone URL; system Python can't read ~/Documents).
- **Photobooth cropped to a single strip.** Source files are 1200×1800 print sheets: the same 2×6 strip twice on kraft paper. Measured: halves identical in content (diffs are sub-pixel resampling only), no white margin anywhere, strip frame is kraft and part of the design, banner wider than the photos. Crop = exact left half, 600×1800, configured in `CROP` in `ingest.js`; the original in storage is the cropped strip too. All 70 regenerated via `--regenerate`, 0 failures, no orphans. Source sheets untouched.
- **Both galleries fully ingested, 0 failures.** Photobooth 70 rows (all 600×1800). Photographer 294 rows (178 landscape, 116 portrait, 18 distinct sizes). 364 rows total, no NULLs, `sort_order` contiguous and chronological, every original verified GPS-free with camera EXIF kept.
- **Stages 3–4: `booth.html` (CSS grid) and `photos.html` (justified rows)** on shared `assets/site.css` (RSVP tokens verbatim) + `assets/gallery.js`. Grid verified at 390/768/1440/1920: 2/3/5 columns. Justified rows pack to a target height (180 mobile / 260 desktop) and fill the width exactly; verified 294 photos, 66 rows, every edge on the container width, no overlaps. Lightbox with keys/swipe/Esc, focus return, `?download=` links confirmed to return attachment headers.
- **Chapters + pill on `photos.html`.** Seven sections by `taken_at` (UTC, to the second) from a JSON block in the page; headings + counts derived from one capture-time fetch, rendered before any photo (35/33/102/30/26/49/19 = 294). Floating pill names the chapter in view, opens a sheet of all chapters (current in lavender), right zone is back-to-top; Escape/backdrop/close/selection close it; booth gets the up-arrow-only pill. Loading is by block of 40 at any index: a jump loads its own chapter first, the rest fills in on scroll, growth above the viewport is scroll-compensated (`overflow-anchor:none`). `#chapter-N` deep links work.
- **Stage 5: select mode + streamed zip.** Select toggle, checkbox overlay in both layouts, bar with live `N selected · 67 MB` (sizes via HEAD on originals), Select all shown = loaded tiles only (sparse `photos`), Clear, Cancel, progress. Zips **original JPGs** with vendored client-zip 2.5.0, streamed to disk: picker (Chromium) → OPFS via `zip-writer.worker.js` (Safari/iOS/Firefox) → Blob capped 150 MB. 2 GB cap. **Verified in desktop Safari**: 17.9 MB zip saved to ~/Downloads, `unzip -t` clean. Alt text floor on tiles + lightbox: `Photo 43 of 294 — Party and Portraits`.
- **Stage 6: archives + Download all.** `ingest.js --archives` builds web + originals zips per gallery from storage (archiver, streamed, uploaded via `fs.openAsBlob`), verifies served size; a full ingest rebuilds them itself. Built: photobooth 20.0 / 34.9 MB, photographer 69.6 MB / 1.04 GB, `unzip -t` clean. Pages show one button, "Download all · 1.0 GB", serving the **originals** zip (web zips unsurfaced), size read from the object at load. Over-cap note links to it.
- **Stage 7 + landing: guest uploads.** `assets/upload.js` (shared, mounts into `#uploader`) inline on `index.html` after the note and gallery cards, and standalone at `/upload`. Resumable TUS via vendored tus-js-client 4.3.1, direct storage hostname, 6 MB chunks, `submissions` bucket (2 GB limit confirmed). Per-file % bar, running total, 2 GB + media-type guards, `uploads` row per file via PostgREST, `beforeunload`, per-file Retry that resumes. **Resume verified:** 120 MB upload, 8 s simulated outage at 30% → one HEAD, continued from 30%, single creation request. Test data removed. `index.html`: note, anchored speech ask, cards with live counts and covers (`COVER` in the page), then the form.
- **Stage 9 (assets + meta):** favicon.ico / favicon-32.png / favicon-16.png / apple-touch-icon.png copied from the RSVP site; `og-image.jpg` generated from the landing cover (photographer sort_order 4) at 1200×630, 134 KB JPEG. Every page audited: `noindex`, `theme-color`, absolute `og:url` / `og:image` / `twitter:image` on `https://thanks.francismroberts.com/`.

## Decisions that differ from the spec
- Photo `id` is a UUID from the source file's SHA-256, for idempotency. 2x thumb at `<gallery>/thumb/<id>@2x.webp`. Originals keep real extension. Live `photos` table has `original_path` (not in spec SQL). Capture time falls back EXIF → filename `YYYYMMDD_HHMMSS` → mtime. Existing rows self-heal on re-run; `--limit 0` runs only that pass.
- Admin key is the modern `sb_secret_` key. Node 26 needs Buffers, not paths, into sharp/exifr; `engines` pinned `>=20 <27`.
- Pages use plain `fetch` to PostgREST, no supabase-js anywhere. Download filenames carry the gallery slug (`francis-grad-party-booth-001.jpg`).
- Lightbox and Download all both serve originals (JPG), not WebP. Tier 2 streams to disk instead of in-memory JSZip; StreamSaver rejected (buffers on Safari). Photographer set is 294 / 1.0 GB, not ~300 / 1.1 GB.

## Broken or unfinished
- DNS for `thanks.francismroberts.com` and the GitHub Pages custom-domain setting not yet confirmed live; `CNAME` is in the repo.
- Local-only test hooks in `gallery.js` (`?zipvia=`, `?selftest-zip=`) are hostname-gated. Archives are CDN-cached; a rebuild may serve old bytes briefly. iOS untested on a device.

## Next session should start with
1. Confirm GitHub Pages is serving the repo at `thanks.francismroberts.com` (DNS CNAME → GitHub, HTTPS enforced), then check an OG preview with a real share (iMessage, WhatsApp).
2. On a real iPhone over HTTPS: select-mode zip (OPFS path, 5 photos → Files) and a real camera-roll upload (HEIC/MOV).
3. Landing copy and cover choices are first drafts.
